# Org tagging reference

General note for any team, not specific to carepulse or chartvault: the four
required tag/label keys (`application`, `environment`, `managed-by`,
`data-class`) come from Platform Engineering's Common Application
Requirements page. This file is just a pointer for anyone browsing the repo —
it isn't consumed by either app's Terraform.
